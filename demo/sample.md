# 欢迎使用 MinMdEditor

这是一份演示文件，覆盖编辑器支持的全部基础语法。编辑器全程保持渲染态：语法标记（#、**、` 等）隐藏，直接看到格式化效果；按 ⌘1~⌘6 可把当前行设为对应级别标题。

## 文字样式

**加粗文字**、*斜体文字*、***加粗斜体***，以及 `行内代码`。

## 列表

- 无序列表项
- 另一个列表项
  - 嵌套列表项

1. 有序列表第一项
2. 有序列表第二项

## 引用

> 这是一段引用。
> 引用的第二行。

## 链接与图片

这是 [一个链接](https://example.com)。

![示例图片](https://picsum.photos/640/360)

## 代码块

```rust
fn main() {
    let greeting = "hello, live preview!";
    println!("{}", greeting);
}
```

```ts
const items: string[] = ["a", "b", "c"];
items.map((x) => x.toUpperCase()).forEach(console.log);
```

## 分隔线

---

## 关于打字机模式

光标所在行会尽量保持在屏幕垂直方向的中间位置。向下一直按方向键，体会页面自动滚动的效果。
